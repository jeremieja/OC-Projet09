# Dépendances MINIMALES pour déployer le dashboard sur Streamlit Community Cloud.
# Volontairement léger : pas de torch / transformers / setfit (plusieurs Go, inutiles
# ici car la prédiction live utilise le modèle TF-IDF sauvegardé + l'API Ministral).
streamlit>=1.40
scikit-learn>=1.4
pandas>=2.0
numpy>=1.24
plotly>=5.20
joblib>=1.3
wordcloud>=1.9
matplotlib>=3.7
httpx>=0.27
python-dotenv>=1.0
