# Dépendances pour le DASHBOARD déployé (Streamlit Community Cloud).
# Volontairement LÉGER : pas de torch / transformers / setfit (plusieurs Go, inutiles
# en production car la prédiction live utilise le modèle TF-IDF sauvegardé + l'API Ministral).
#
# Pour reproduire les EXPÉRIENCES (entraînement des modèles lourds), voir requirements-dev.txt
streamlit>=1.40
scikit-learn>=1.4
pandas>=2.0
numpy>=1.24
plotly>=5.20
joblib>=1.3
wordcloud>=1.9
matplotlib>=3.7
httpx>=0.27
tqdm>=4.65
python-dotenv>=1.0
